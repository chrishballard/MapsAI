---
phase: 14-score-library-dependencies
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - vitest.config.mts
  - package.json
  - src/lib/optimization-score.ts
  - tests/lib/optimization-score.test.ts
autonomous: true
requirements:
  - SCORE-LIB
must_haves:
  truths:
    - "computeOptimizationScore returns total 0-100, grade green/amber/red, and 5 checks"
    - "Score thresholds are green >= 70, amber 40-69, red < 40"
    - "Each of 5 signals contributes 0-20 points with equal weighting"
    - "Time-based signals use a 30-day rolling window"
    - "Score function is a pure function with no framework imports"
  artifacts:
    - path: "src/lib/optimization-score.ts"
      provides: "Pure optimization score function"
      exports: ["computeOptimizationScore", "OptimizationScore", "ScoreCheck", "ScoreGrade", "ScoreStatus", "ProfileInput"]
    - path: "tests/lib/optimization-score.test.ts"
      provides: "Unit tests for all 5 signals and 3 grade bands"
      min_lines: 50
    - path: "vitest.config.mts"
      provides: "Vitest configuration with tsconfig paths"
      min_lines: 8
  key_links:
    - from: "tests/lib/optimization-score.test.ts"
      to: "src/lib/optimization-score.ts"
      via: "import { computeOptimizationScore } from '@/lib/optimization-score'"
      pattern: "import.*computeOptimizationScore.*from.*@/lib/optimization-score"
---

<objective>
Create the pure optimization score function with full TDD coverage.

Purpose: The score function is the single source of truth for profile optimization scoring across all downstream UI phases (15, 16, 17). Building it with TDD ensures the scoring contract is locked and tested before any consumer depends on it.
Output: `src/lib/optimization-score.ts` with unit tests, vitest configured and passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-score-library-dependencies/14-CONTEXT.md
@.planning/phases/14-score-library-dependencies/14-RESEARCH.md
@.planning/phases/14-score-library-dependencies/14-UI-SPEC.md

<interfaces>
<!-- Score function input/output contract from RESEARCH.md and UI-SPEC.md -->

ProfileInput shape (plain interface, NO Prisma imports):
```typescript
export interface ProfileInput {
  reviews: Array<{ rating: number; reviewDate: Date }>;
  posts: Array<{ publishedAt: Date | null; status: string }>;
  descriptions: Array<{ isApproved: boolean; isPushed: boolean }>;
  services: Array<{ isApproved: boolean; isPushed: boolean }>;
}
```

Return type:
```typescript
export interface OptimizationScore {
  total: number;          // 0-100
  grade: ScoreGrade;      // 'green' | 'amber' | 'red'
  checks: ScoreCheck[];   // one entry per active signal (5 items)
}

export interface ScoreCheck {
  signal: string;        // e.g. "Review Frequency"
  score: number;         // 0-20
  max: number;           // always 20
  status: ScoreStatus;   // 'good' | 'warning' | 'critical'
  value: string;         // e.g. "3 reviews in 30 days"
  benchmark: string;     // e.g. "4+ reviews per 30 days"
  recommendation: string;
}
```

Existing lib pattern (from src/lib/keyword-generator.ts, src/lib/description-generator.ts):
- Plain TypeScript module with named exports
- No framework imports, no side effects
- Functions accept data objects, return computed results
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install vitest and configure test infrastructure</name>
  <files>package.json, vitest.config.mts</files>
  <read_first>
    - /Users/christopherballlard/Projects/MapsAI/package.json
    - /Users/christopherballlard/Projects/MapsAI/tsconfig.json
  </read_first>
  <action>
1. Install vitest dev dependencies:
   ```
   npm install -D vitest @vitejs/plugin-react vite-tsconfig-paths
   ```

2. Add test script to package.json scripts:
   ```json
   "test": "vitest run",
   "test:watch": "vitest"
   ```

3. Create `vitest.config.mts` in project root with this exact content:
   ```typescript
   import { defineConfig } from 'vitest/config';
   import react from '@vitejs/plugin-react';
   import tsconfigPaths from 'vite-tsconfig-paths';

   export default defineConfig({
     plugins: [tsconfigPaths(), react()],
     test: {
       environment: 'node',
       include: ['tests/**/*.{test,spec}.{ts,tsx}'],
       globals: true,
     },
   });
   ```

4. Create empty `tests/lib/` directory structure:
   ```
   mkdir -p tests/lib
   ```
  </action>
  <verify>
    <automated>cd /Users/christopherballlard/Projects/MapsAI && npx vitest run --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "vitest" in devDependencies
    - package.json scripts contains "test": "vitest run"
    - vitest.config.mts exists in project root
    - vitest.config.mts contains "environment: 'node'"
    - vitest.config.mts contains "tsconfigPaths()"
    - `npx vitest run --passWithNoTests` exits 0
  </acceptance_criteria>
  <done>Vitest is installed, configured with tsconfig path aliases, and runs successfully with zero tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD the optimization score function</name>
  <files>tests/lib/optimization-score.test.ts, src/lib/optimization-score.ts</files>
  <read_first>
    - /Users/christopherballlard/Projects/MapsAI/src/lib/keyword-generator.ts
    - /Users/christopherballlard/Projects/MapsAI/src/lib/description-generator.ts
    - /Users/christopherballlard/Projects/MapsAI/.planning/phases/14-score-library-dependencies/14-UI-SPEC.md
    - /Users/christopherballlard/Projects/MapsAI/.planning/phases/14-score-library-dependencies/14-RESEARCH.md
  </read_first>
  <behavior>
    RED phase -- write failing tests first:
    - Test: perfect profile (10 recent reviews rated 5, 5 recent posts, approved+pushed description, 5 approved+pushed services) returns total=100, grade='green', checks.length=5
    - Test: empty profile (no reviews, no posts, no descriptions, no services) returns total < 40, grade='red', checks.length=5
    - Test: partial profile (2 signals passing, 3 failing) returns grade='amber' (total 40-69)
    - Test: grade boundary -- total=70 returns grade='green', total=69 returns grade='amber', total=40 returns grade='amber', total=39 returns grade='red'
    - Test: review frequency signal -- 0 reviews in 30d = score 0, 2 reviews = partial score, 4+ reviews = score 20
    - Test: post frequency signal -- 0 posts in 30d = score 0, 2 posts = partial score, 4+ posts = score 20
    - Test: rating signal -- no reviews = score 0, avg < 3.0 = low score, avg 3.0-3.9 = partial, avg >= 4.0 = score 20
    - Test: description completeness -- no descriptions = score 0, approved but not pushed = partial, approved+pushed = score 20
    - Test: services completeness -- 0 pushed = score 0, 1-2 pushed = partial, 3+ pushed = score 20
    - Test: only reviews within 30-day window are counted for frequency
    - Test: each check has signal, score, max (20), status, value, benchmark, recommendation fields

    GREEN phase -- implement to pass all tests:

    Score function contract (per D-01 through D-08):
    - 5 signals, 20 points each, 100 total (D-01, D-02)
    - NO images signal (D-03), NO attributes signal (D-04)
    - Thresholds: green >= 70, amber 40-69, red < 40 (D-05)
    - Input: ProfileInput plain interface (D-06) -- NOT Prisma types
    - Return: { total, grade, checks } (D-07)
    - 30-day rolling window for time-based signals (D-08)

    Signal benchmarks (Claude's discretion, per RESEARCH.md Open Question 1):
    - Review Frequency: good >= 4 in 30d (20pts), warning 1-3 (proportional 5-15pts), critical 0 (0pts)
    - Post Frequency: good >= 4 in 30d (20pts), warning 1-3 (proportional 5-15pts), critical 0 (0pts)
    - Rating: good >= 4.0 (20pts), warning 3.0-3.9 (proportional 10-19pts), critical < 3.0 or no reviews (0-9pts)
    - Description: good = isPushed true (20pts), warning = isApproved but not pushed (10pts), critical = neither (0pts)
    - Services: good >= 3 pushed (20pts), warning 1-2 pushed (proportional 7-13pts), critical 0 (0pts)

    Exact copy strings from UI-SPEC Score Signal Copy Contract:
    - Review Frequency benchmark: "4+ reviews per 30 days"
    - Review Frequency recommendation: "Encourage happy customers to leave a review after service."
    - Post Frequency benchmark: "4+ posts per 30 days"
    - Post Frequency recommendation: "Publish at least 1 Google post per week to stay active."
    - Rating benchmark: "4.0+ average rating"
    - Rating recommendation: "Respond to all reviews and resolve complaints promptly."
    - Description benchmark: "Description approved and live on GBP"
    - Description recommendation: "Approve and push the AI-generated description from the optimization page."
    - Services benchmark: "3+ services live on GBP"
    - Services recommendation: "Add or approve more services to show Google your full offering."

    Code comments MUST include: "Score thresholds: green >= 70, amber 40-69, red < 40" (per ROADMAP SC-4).
  </behavior>
  <action>
1. RED: Create `tests/lib/optimization-score.test.ts` with all test cases listed in behavior block. Import from `@/lib/optimization-score`. Run `npx vitest run` -- all tests MUST fail (module not found).

2. GREEN: Create `src/lib/optimization-score.ts` with:
   - Export types: `ScoreStatus = 'good' | 'warning' | 'critical'`, `ScoreGrade = 'green' | 'amber' | 'red'`
   - Export interfaces: `ScoreCheck`, `OptimizationScore`, `ProfileInput` (as specified in context interfaces block)
   - Export function `computeOptimizationScore(profile: ProfileInput): OptimizationScore`
   - Constant `WINDOW_DAYS = 30`
   - 5 internal scoring functions: `scoreReviewFrequency`, `scorePostFrequency`, `scoreRating`, `scoreDescriptionCompleteness`, `scoreServicesCompleteness`
   - Each returns a `ScoreCheck` with the exact benchmark and recommendation strings from the UI-SPEC copy contract
   - Comment at top of file: `// Score thresholds: green >= 70 | amber 40-69 | red < 40`
   - NO imports from `@/generated/prisma` or any framework module
   - Run `npx vitest run` -- all tests MUST pass.

3. REFACTOR: Review for clarity. Ensure all 5 signal names match UI-SPEC exactly: "Review Frequency", "Post Frequency", "Rating", "Description Completeness", "Services Completeness".
  </action>
  <verify>
    <automated>cd /Users/christopherballlard/Projects/MapsAI && npx vitest run tests/lib/optimization-score.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/optimization-score.ts contains "export function computeOptimizationScore"
    - src/lib/optimization-score.ts contains "export interface ProfileInput"
    - src/lib/optimization-score.ts contains "export interface OptimizationScore"
    - src/lib/optimization-score.ts contains "export interface ScoreCheck"
    - src/lib/optimization-score.ts contains "Score thresholds: green >= 70"
    - src/lib/optimization-score.ts contains "4+ reviews per 30 days"
    - src/lib/optimization-score.ts contains "4+ posts per 30 days"
    - src/lib/optimization-score.ts contains "4.0+ average rating"
    - src/lib/optimization-score.ts contains "3+ services live on GBP"
    - src/lib/optimization-score.ts does NOT contain "from '@/generated/prisma'"
    - src/lib/optimization-score.ts does NOT contain "from 'react'"
    - tests/lib/optimization-score.test.ts contains at least 8 test cases (it blocks)
    - `npx vitest run tests/lib/optimization-score.test.ts` exits 0 with all tests passing
  </acceptance_criteria>
  <done>Score function is fully implemented with all 5 signals, correct thresholds, exact copy strings from UI-SPEC, and comprehensive unit test coverage. All tests pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run` exits 0 with all tests passing
- `src/lib/optimization-score.ts` is a pure function with no framework imports
- Score thresholds documented in code comments
- All 5 signals produce checks with benchmark and recommendation strings
- Grade boundaries: 70+ = green, 40-69 = amber, <40 = red
</verification>

<success_criteria>
- computeOptimizationScore is exported and callable
- All unit tests pass (covering all 5 signals, 3 grade bands, boundary conditions, 30-day window)
- No Prisma or React imports in the score file
- Downstream phases can `import { computeOptimizationScore, OptimizationScore, ScoreCheck, ProfileInput } from '@/lib/optimization-score'`
</success_criteria>

<output>
After completion, create `.planning/phases/14-score-library-dependencies/14-01-SUMMARY.md`
</output>
